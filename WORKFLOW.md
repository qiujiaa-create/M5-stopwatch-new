# 本项目的最小开发约定

文档版本：0.1；最后更新：2026-09-16 22:34（中国时间）。

每项改动只需写清五件事：**目标、修改范围、不能碰的链路、验证动作、回退到哪个提交**。把构建通过、安装或刷写通过、实机操作通过分开记录。

- `main` 保存经过验收的基线和清楚标注的初始导入。新功能和修复在独立分支或 worktree 中做。
- 同一 worktree 同一时间只允许一个 AI 修改。另一 AI 可在独立 worktree 审查差异或处理另一项任务。
- 合并前查看具体文件差异，确认没有顺手修改分区表、HID 报告、BLE 音频或凭据。保留不相关改动。
- 运行中的 Bridge App、手表 factory 与 ota_0 都是部署结果，不能仅凭源码文件或日志推断已更新。记录构建产物哈希和实际设备行为。
- `local-reference/` 只供本机参考；账号、令牌、Wi-Fi、签名资料及运行配置不进入 Git。

首次建议任务：**V2 推理滑动与中心四向触摸修复**。验收至少覆盖向左/向右的实际推理等级变化、四个方向与松手归中、BLE 重连，以及 A/B 语音不回退。旧文档中的“已完成”不能替代本次实机结果。
